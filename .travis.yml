sudo: false
language: c++
addons:
  apt:
    packages:
    - libopenmpi-dev
    - libboost-system-dev
    - libboost-system-dev
    - libboost-filesystem-dev
    - libgsl0-dev
compiler:
  - gcc
script:
  - make all
